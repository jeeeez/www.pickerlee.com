// const path = require('path');
// const template = require('art-template');

// template.config('base', path.resolve(__dirname, '../views'));

// const dateFormatter = require('date-formatter/dist/date-formatter');

// template.helper('dateFormatter', (timestamp, format) => {
// 	return dateFormatter(new Date(timestamp), format);
// });


// module.exports = template;
